# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# lockfiles de otros gestores
package-lock.json
yarn.lock

# --- Secretos (solo .env.example va al repo, vacío) ---
.env
.env.*
!.env.example

# Auth OAuth — URLs del proyecto cloud (ver supabase-auth.local.md, no subir)
supabase-auth.local.md
*.auth.local.md

# Google OAuth — credenciales descargadas de Google Cloud (no subir)
client_secret*.json
.local/

# vercel
.vercel

# Pitch local del equipo (vive en ../pitch/, no en public/)
/public/pitch/

# QR imprimibles (se generan en ../pitch/qr/)
/qr/

# hardhat
/blockchain/cache

# typescript
*.tsbuildinfo
next-env.d.ts
